version: '2'
services:
  recipebook-mongodb:
    image: mongo:4.2.6
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/recipebook-mongodb-config --bind_ip 0.0.0.0
  recipebook-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  recipebook-mongodb-config:
    image: mongo:4.2.6
    container_name: recipebook-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
